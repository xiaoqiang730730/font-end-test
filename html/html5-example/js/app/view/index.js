define(['jquery'], function ($) {
    var index = {
        // 初始化
        init: function (news, demand, circle) {
            
        },
        
        // 刷新
        refresh : function (news, demand, circle) {
            
        },
        btn_click: function(func) {

        }
    };

    return index;
});