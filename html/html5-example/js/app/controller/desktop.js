define(['jquery', 'system'], function ($) {
    console.log('desktop');
    
    var desktop = {};
    
    // 桌面初始化, 有系统调用
    desktop.init = function () {
        
    };
    
    desktop.startMenu = {
        add: function (params) {
            // params = {icon, title, url, type, h, w}
        },
        
        del: function (menu) {
            // del 
        },
        
    };
    
    
    return desktop;
});